# ============================================================================
# AI AGENT MODULE - Environment Variables
# ============================================================================
# Kopieer dit bestand naar .env.local en vul je eigen waarden in

# AI Agent Configuration
# -----------------------
# Webhook secret voor VPS communicatie (genereer een random string)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AGENT_WEBHOOK_SECRET=your-secret-key-change-this-123456789

# Encryption key voor credentials vault (MOET exact 32 characters zijn)
# Run: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
CREDENTIALS_ENCRYPTION_KEY=your-32-byte-encryption-key!!

# VPS Configuration
# -----------------
# Je Render VPS URL (of andere VPS provider)
# Voorbeeld: https://your-vps-name.onrender.com
VPS_API_URL=https://your-render-vps.onrender.com

# VPS API secret (moet matchen met wat op je VPS staat)
# Dit is voor authenticatie tussen WritGo.nl en je VPS
VPS_API_SECRET=your-vps-api-secret-key

# VPS enabled (zet op true als VPS klaar is, false om alleen UI te testen)
VPS_ENABLED=false

# ============================================================================
# BESTAANDE ENVIRONMENT VARIABLES
# ============================================================================
# Deze heb je al, maar ter referentie:

# Supabase
# NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AIML API (voor Claude Opus)
# AIML_API_KEY=your-aiml-api-key

# Stripe
# STRIPE_SECRET_KEY=your-stripe-secret-key
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# ============================================================================
# GENEREER VEILIGE KEYS
# ============================================================================
# Run deze commands in je terminal:

# Voor AGENT_WEBHOOK_SECRET:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Voor CREDENTIALS_ENCRYPTION_KEY:
# node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"

# Voor VPS_API_SECRET:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
