version: '3.8'

services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: writgo-agent-runtime
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - WRITGO_API_URL=${WRITGO_API_URL}
      - WRITGO_WEBHOOK_SECRET=${WRITGO_WEBHOOK_SECRET}
      - MAX_ITERATIONS=${MAX_ITERATIONS:-50}
      - SANDBOX_TIMEOUT=${SANDBOX_TIMEOUT:-300}
    volumes:
      - ./src:/app/src
      - ./config:/app/config
      - ./logs:/app/logs
      - ./workspace:/app/workspace
      - /var/run/docker.sock:/var/run/docker.sock  # For sandbox management
    restart: unless-stopped
    networks:
      - agent-network

  redis:
    image: redis:7-alpine
    container_name: writgo-agent-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - agent-network

volumes:
  redis-data:

networks:
  agent-network:
    driver: bridge
